<li> 
	<%= gravatar_for user %> 
	<%= link_to user.name, user %>
	<% if !current_user?(user) && current_user.admin %> 
	 <p><%= link_to "delete", user, method: :delete, data: { confirm: "You sure?" } %></p>
	<%end%>

</li>
